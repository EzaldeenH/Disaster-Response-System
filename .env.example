# Disaster Response System - Environment Configuration
# Copy to '.env' and fill in values. Never commit the real .env file.

# SQL Server SA password (min 8 chars, upper+lower+digit+symbol)
DB_PWD=Disaster@2026

# CORS origin - the URL where the frontend is served
# Local testing:          http://localhost:8081
# Dokploy (nip.io):       http://drs-frontend.158.158.72.65.nip.io
CORS_ORIGIN=http://localhost:8081
