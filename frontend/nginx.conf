server {
    listen 80;

    # Traefik (Dokploy's edge router) handles /api routing and TLS.
    # This nginx only serves the Angular SPA static files.

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
